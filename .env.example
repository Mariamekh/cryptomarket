# Force the mocked dataset instead of the live CoinGecko API.
# Useful for offline work, demos, or when the free tier is rate-limiting.
# Copy to `.env.local` and set to `true` to enable.
VITE_USE_MOCK=false
